# Setup Instructions

1. Set environment variables before running R:
   - Windows: Set via System Properties or run `Sys.setenv(DB_USER="...", DB_PASSWORD="...")`
   - Mac/Linux: Add to `~/.bashrc` or `~/.zshrc`:
     ```bash
     export DB_USER="your_username"
     export DB_PASSWORD="your_password"
     ```
